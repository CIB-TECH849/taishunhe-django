services:
  - type: web
    name: taishunhe-backend
    plan: starter
    env: docker
    region: singapore

    dockerfilePath: ./Dockerfile

    envVars:
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: taishunhe-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: taishunhe-redis
          property: connectionString

databases:
  - name: taishunhe-db
    plan: starter
    region: singapore

services:
  - name: taishunhe-redis
    type: redis
    plan: starter
    maxmemoryPolicy: allkeys-lru